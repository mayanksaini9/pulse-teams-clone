{
  "name": "teams-clone-root",
  "version": "1.0.0",
  "description": "Root workspace runner for Pulse Teams Clone",
  "scripts": {
    "install-all": "npm install --include=dev --prefix server && npm install --include=dev --prefix client",
    "build": "npm run build --prefix client",
    "start": "node server/server.js",
    "server": "npm run start --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run start --prefix server\" \"npm run dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
